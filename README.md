Initial Configuration of Grunt Serve https://www.npmjs.com/package/grunt-serve

## Installation

At the command prompt, install it using npm

```
npm install -g grunt
npm install -g grunt-cli

npm install
grunt serve
```
